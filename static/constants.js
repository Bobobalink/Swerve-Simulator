// width and height of the robot driving around area in pixels
const SCREEN_WIDTH = 600; // decrease this to shrink the width of the drivable area
const SCREEN_HEIGHT = 600; // decrease this to shrink the height of the drivable area

// pixels per meter, for drawing the robot itself
const POSITION_SCALE_FACTOR = 75.0; // decrease this to make the robot appear smaller

// pixels per (meter per second)
const VELOCITY_SCALE_FACTOR = 8.0; // decrease this to make the arrows appear shorter
